# DailyLumen 运行时依赖：无（纯 Python 标准库，Python 3.10+）

# 跑本项目不需要安装任何包：
#   python -m review_tool doctor
#   python -m unittest discover -s tests -t .

# 开发 / CI 可选依赖（不影响运行时零依赖）：
#   pytest  —— 若偏好 pytest 而非 unittest
#   ruff    —— 代码风格检查与格式化（配置见 pyproject.toml）
#
#   pip install pytest ruff
